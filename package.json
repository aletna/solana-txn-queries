{
  "name": "get-solana-txn-data",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lfg": "npx tsc --outDir dist && node dist/index.js",
    "get-tx": "npx tsc --outDir dist && node dist/index.js getTx",
    "get-txns": "npx tsc --outDir dist && node dist/index.js getTxs",
    "get-sigs": "npx tsc --outDir dist && node dist/index.js getSignatures"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@solana/web3.js": "^1.44.0",
    "make-runnable": "^1.3.10",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.3"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.2"
  }
}
